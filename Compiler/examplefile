conventions {
	arr
}

main {
	hello {
		print : "hello"
		input
		if : input = "hello"
			then {
				print : "yo"
			} else-if {
				if : input = "hey"
					then {
						print : "sup"
			}
			}
			else {
				print : "say waaaaa?"
			}
	}
}